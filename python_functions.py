#trying out making functions in python

#def tally(x,y):  #def means define, defining what the function will do
    #z = x + y
    #return z
    
#a = float(input('Enter num: '))
#b = float(input('Enter num: '))
#c = tally (a,b)
#print (c)



#def tally (num,arr):  #making a function that can find the sum of all the items inside an array
    #total = 0           # the variable name in parameter doesnt have to be the same
    #for i in range(0,num,1):
        #total = total + arr[i]
    #return total

#number = 6
#a = [5,5,5,5,5,5]
#sum = tally (number,a)
#print(sum)



#def tally (x,y):
    #sum = x + y
    #diff = x - y
    #prod = x * y
    #quot = x / y
    #return sum , diff, prod, quot #apparently you can return many things at the same time in python

#a = float(input('Enter num: '))
#b = float(input('Enter num: '))

#w,x,y,z = tally (a,b) #need to initialize same number of variables based on the return of the function
#print(w,x,y,z)



#def tally (num): #making a function that can pass an array and also store numbers inside it
    #x = []
    #for i in range (0,num,1):
        #numbers = float(input('Enter a number: '))
        #x.append(numbers)
    #return x

#number = int(input('Enter a number: '))
#y = tally (number)
#print (y)




